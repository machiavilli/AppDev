import { ImageSourcePropType } from 'react-native';

// Image assets
const IMG = {
  LOGO: require('../../asset/img/logo.png') as ImageSourcePropType,
  // Add more images as needed
  USER_AVATAR: require('../../asset/img/user-avatar.png') as ImageSourcePropType,
  APP_ICON: require('../../asset/img/app-icon.png') as ImageSourcePropType,
};

export default IMG;
